<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Turis_Travel</title>

    <!-- Bootstrap -->
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/Turis_Travel.styles.css" asp-append-version="true" />
    


    <style>
        /* === NAVBAR === */
        .navbar {
            background: linear-gradient(90deg, #00b4d8, #0096c7, #0077b6);
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.15);
        }

        .navbar-brand {
            font-weight: 800;
            color: #fff !important;
            font-size: 1.6rem;
            letter-spacing: 1px;
            text-shadow: 1px 1px 5px rgba(0, 0, 0, 0.3);
        }

        .nav-link {
            color: #e0f7fa !important;
            font-weight: 600;
            transition: all 0.3s ease;
            border-radius: 8px;
            padding: 8px 14px !important;
        }

        .nav-link:hover,
        .dropdown-item:hover {
            background-color: rgba(255, 255, 255, 0.15);
            color: #ffeb3b !important;
            transform: translateY(-2px);
        }

        .dropdown-menu {
            border: none;
            border-radius: 10px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
        }

        html, body {
            height: 100%;
            margin: 0;
            display: flex;
            flex-direction: column;
            font-family: 'Segoe UI', sans-serif;
            background: linear-gradient(to bottom right, #f4fbff, #e9f8f5);
        }

        main {
            flex: 1;
        }

        /* === CARRUSEL === */
        .carousel-item {
            height: 100vh;
            position: relative;
        }

        .carousel-item img {
            height: 100%;
            width: 100%;
            object-fit: cover;
            filter: brightness(0.6);
        }

        .carousel-caption {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            color: #fff;
            text-align: center;
            background: rgba(0, 0, 0, 0.35);
            padding: 45px 50px;
            border-radius: 20px;
            max-width: 850px;
            width: 90%;
            box-shadow: 0 0 25px rgba(0, 0, 0, 0.5);
            animation: fadeIn 2s ease;
        }

        .carousel-caption h1 {
            font-size: 3rem;
            font-weight: 800;
            margin-bottom: 15px;
            color: #fff;
            text-shadow: 2px 2px 8px rgba(0,0,0,0.6);
        }

        .carousel-caption p {
            font-size: 1.2rem;
            margin-bottom: 20px;
            color: #f1f1f1;
        }

        .carousel-caption h4 {
            font-size: 1.5rem;
            margin-top: 15px;
            color: #ffeb3b;
        }

        .features {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 20px;
            margin-top: 30px;
        }

        .feature {
            background: rgba(255, 255, 255, 0.15);
            padding: 15px 25px;
            border-radius: 12px;
            font-weight: 500;
            backdrop-filter: blur(6px);
            box-shadow: 0 0 10px rgba(0,0,0,0.3);
        }

        /* === FOOTER === */
        .footer {
            background: linear-gradient(90deg, #0077b6, #0096c7, #00b4d8);
            color: #fff;
            text-align: center;
            padding: 20px 10px;
            font-size: 0.95rem;
            letter-spacing: 0.5px;
            margin-top: auto;
            box-shadow: 0 -3px 10px rgba(0,0,0,0.2);
        }

        .footer a {
            color: #ffeb3b;
            text-decoration: none;
            font-weight: 600;
        }

        .footer a:hover {
            text-decoration: underline;
        }

        /* === SCROLLBAR === */
        ::-webkit-scrollbar {
            width: 8px;
        }

        ::-webkit-scrollbar-thumb {
            background: linear-gradient(180deg, #00b4d8, #0077b6);
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: linear-gradient(180deg, #0096c7, #023e8a);
        }
    </style>
</head>
<body>
    <!-- === NAVBAR === -->
    <header>
        <nav class="navbar navbar-expand-sm navbar-dark border-bottom mb-0">
            <div class="container-fluid">
                <a class="navbar-brand" asp-controller="Home" asp-action="Index">
                    <i class="bi bi-globe-americas me-2"></i>Turis_Travel
                </a>

                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav ms-auto">

                        <li class="nav-item">
                            <a class="nav-link" asp-controller="Home" asp-action="Index">
                                <i class="bi bi-house-door me-1"></i>Inicio
                            </a>
                        </li>

                        <li class="nav-item">
                            <a class="nav-link" asp-controller="Configuracion" asp-action="Index">
                                <i class="bi bi-gear me-1"></i>Configuración
                            </a>
                        </li>

                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" id="menuGestion" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                <i class="bi bi-briefcase me-1"></i>Gestión
                            </a>
                            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="menuGestion">
                                <li>
                                    <a class="dropdown-item" asp-controller="Paquetes" asp-action="Index">
                                        <i class="bi bi-box-seam"></i> Paquetes Turisticos
                                    </a>
                                </li>
                                <li>
                                    <a class="dropdown-item" asp-controller="Usuario" asp-action="Index">
                                        <i class="bi bi-people me-1"></i> Usuarios
                                    </a>
                                </li>
                            </ul>
                        </li>

                        <li class="nav-item">
                            <a class="nav-link" asp-controller="Home" asp-action="Privacy">
                                <i class="bi bi-shield-check me-1"></i>Privacidad
                            </a>
                        </li>

                        <li class="nav-item">
                            <a class="nav-link text-dark" asp-area="" asp-controller="Login" asp-action="Index">
                                Inicio de Sesión
                            </a>
                        </li>

                    </ul>
                </div>
            </div>
        </nav>
    </header>

    <!-- === CARRUSEL (solo en página de inicio) === -->
    @if (ViewData["Title"]?.ToString() == "Inicio")
    {
        <div id="carouselTurisTravel" class="carousel slide" data-bs-ride="carousel">
            <div class="carousel-inner">
                <!-- SLIDE 1 -->
                <div class="carousel-item active">
                    <img src="https://images.unsplash.com/photo-1507525428034-b723cf961d3e" alt="Turismo 1">
                    <div class="carousel-caption">
                        <h1>Bienvenido a TurisTravel</h1>
                        <p>Explora destinos, paquetes y reservas en un solo lugar</p>
                        <h4>¿Por qué elegir TurisTravel?</h4>
                        <p>Ofrecemos experiencias inolvidables con una gestión moderna de roles, permisos y usuarios.</p>
                        <div class="features">
                            <div class="feature">🌍 Destinos Internacionales</div>
                            <div class="feature">👥 Gestión de Usuarios</div>
                            <div class="feature">🧭 Viajes Personalizados</div>
                        </div>
                        <h5 class="mt-4">TurisTravel</h5>
                        <p>Descubre el mundo con nosotros 🌎</p>
                    </div>
                </div>
                <!-- SLIDE 2 -->
                <div class="carousel-item">
                    <img src="https://images.unsplash.com/photo-1526778548025-fa2f459cd5c1" alt="Turismo 2">
                    <div class="carousel-caption">
                        <h1>Descubre nuevas aventuras</h1>
                        <p>Viaja con nosotros a los destinos más impresionantes del planeta</p>
                        <h4>¡Haz tu reserva hoy!</h4>
                        <p>Con TurisTravel, el viaje de tus sueños está a un clic de distancia ✈</p>
                    </div>
                </div>
                <!-- SLIDE 3 -->
                <div class="carousel-item">
                    <img src="https://images.unsplash.com/photo-1500530855697-b586d89ba3ee" alt="Turismo 3">
                    <div class="carousel-caption">
                        <h1>Tu viaje, tu historia</h1>
                        <p>Planea, reserva y disfruta con TurisTravel</p>
                        <h4>Vive experiencias únicas</h4>
                        <p>Transforma tus vacaciones en recuerdos inolvidables 💫</p>
                    </div>
                </div>
            </div>

            <!-- CONTROLES -->
            <button class="carousel-control-prev" type="button" data-bs-target="#carouselTurisTravel" data-bs-slide="prev">
                <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            </button>
            <button class="carousel-control-next" type="button" data-bs-target="#carouselTurisTravel" data-bs-slide="next">
                <span class="carousel-control-next-icon" aria-hidden="true"></span>
            </button>
        </div>
    }

    <!-- === CONTENIDO PRINCIPAL === -->
    <main class="container my-5">
        @RenderBody()
    </main>

    <!-- === FOOTER === -->
    <footer class="footer">
        <div class="container">
            <p class="mb-1">© 2025 <strong>TurisTravel</strong> | Todos los derechos reservados.</p>
            <p class="mb-0">
                <a href="/Privacy" target="_blank"><i class="bi bi-shield-check me-1"></i>Política de Privacidad</a> |
                <a href="/Contacto" target="_blank"><i class="bi bi-envelope me-1"></i>Contáctanos</a> |
                <a href="/SobreNosotros" target="_blank"><i class="bi bi-info-circle me-1"></i>Sobre Nosotros</a>
            </p>
        </div>
    </footer>

    <!-- === SCRIPTS === -->
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
